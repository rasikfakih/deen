// Placeholder for future constants (durations, thresholds) per DEEN Section 7.
// Keep feature-agnostic constants here; feature-specific ones stay in
// lib/features/<feature>/constants.

abstract final class AppConstants {
  static const String appName = 'Deen';
  static const String packageName = 'com.freebuff.deen';
}
